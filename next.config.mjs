/** @type {import('next').NextConfig} */
const nextConfig = {
  // Sitenize telefonunuzdan veya yerel ağdan girmek için kök izin
  allowedDevOrigins: ['192.168.43.93', 'localhost'],
};

export default nextConfig;